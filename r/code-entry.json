{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "code-entry",
  "title": "Code entry",
  "description": "The one-time code surface: one character per box, the whole code reported on the last one so nobody presses a button after typing a code they can see is complete, and a resend that is locked rather than missing while the host is still counting down. It checks nothing and sends nothing. A refused code arrives as `invalid` plus the host's own sentence, which is why an unfinished code looks nothing like a wrong one, and the boxes change stroke, pattern and words together rather than only colour. There is one input behind the boxes, not six: six break paste, break one-time-code autofill and give a screen reader six unlabelled boxes. The countdown does not tick here; `resendIn` is seconds the host owns.",
  "dependencies": [
    "react"
  ],
  "registryDependencies": [
    "@gamestage/tokens",
    "@gamestage/button"
  ],
  "files": [
    {
      "path": "packages/ui/src/code-entry/code-entry.tsx",
      "content": "'use client'\n\nimport * as React from 'react'\n\n// The component brings its own styles. Without this a `gamestage-ui add` installs a\n// working but completely unstyled CodeEntry, and `tsc --noEmit` passes on it,\n// so the gate cannot catch it.\nimport './code-entry.css'\n// The shared rule sets, so the resend control carries the theme's action type\n// and press gesture without this file re-declaring either (GSUI-118).\nimport '../../../tokens/primitives.css'\n\n// The flat install layout puts every component in one directory, so this\n// specifier has to read `./button`. See hud/score.ts, which explains it first.\nimport { Button } from './button'\n\n/**\n * CodeEntry: the one-time code surface. BDS-041.\n *\n * One character per box, the code auto-reported on the last one, and a resend\n * that is unavailable until the host says it is available again.\n *\n * **It checks nothing.** It does not know whether the code is right, it does\n * not send it, and it does not send another one. It reports the characters and\n * the two presses; the host does the rest. A wrong code comes back as `invalid`\n * with the host's own sentence, which is why a code that is simply incomplete\n * looks nothing like one that was refused.\n *\n * There is one input, not six. Six inputs break paste, break the browser's own\n * one-time-code autofill, and give a screen reader six unlabelled boxes to read\n * out. So a single labelled field takes the typing and the boxes are a\n * rendering of it, hidden from the accessibility tree.\n *\n * The countdown does not tick here. `resendIn` is a number of seconds the host\n * owns, because a timer inside a component is state a re-render loses and the\n * host is the one that knows when it last sent a code. While it is above zero\n * the resend control carries `data-gs-locked`: available later, not now, which\n * is exactly what the attribute means everywhere else.\n */\n\nexport interface CodeEntryProps {\n  /** How many characters the code has. Six, in every design so far. */\n  length?: number\n  /** The characters so far, when the host holds them rather than the component. */\n  value?: string\n  /** The player typed. Fires on every character, not only on the last. */\n  onChange?: (value: string) => void\n  /**\n   * Every character is in. Fired once per completed code, which is the\n   * auto-submit the design asks for: nobody presses a button after typing the\n   * last digit of a code they can see is complete.\n   */\n  onComplete?: (value: string) => void\n  /** The headline. \"Check your email\", or whatever this game calls it. */\n  title?: React.ReactNode\n  /** The line under it, which is where the address the code went to belongs. */\n  detail?: React.ReactNode\n  /** What the field is called for a screen reader. */\n  label?: string\n  /** The host refused the code. This component never decides that. */\n  invalid?: boolean\n  /** Why, in the host's own words. Announced with the field, not only shown. */\n  reason?: React.ReactNode\n  /**\n   * Seconds until the host will send another code. Above zero, the resend\n   * control is locked and says how long; at zero or absent, it is available.\n   */\n  resendIn?: number\n  /** The player asked for another code. The host sends it; this never can. */\n  onResend?: () => void\n  /** What the resend control says when it is available. */\n  resendLabel?: string\n  /** The host has been asked and has not answered. Host-declared. */\n  busy?: boolean\n  /** Anything the host wants under the code: another way in, a way back. */\n  children?: React.ReactNode\n}\n\nconst DEFAULTS = {\n  length: 6,\n  title: 'Check your email',\n  label: 'One-time code',\n  resendLabel: 'Send another code',\n} as const\n\n/** \"Resend in 0:42\". Minutes and seconds, because 87 seconds reads as nothing. */\nfunction countdown(seconds: number): string {\n  const whole = Math.max(0, Math.ceil(seconds))\n  const minutes = Math.floor(whole / 60)\n  return `${minutes}:${String(whole - minutes * 60).padStart(2, '0')}`\n}\n\nexport function CodeEntry({\n  length = DEFAULTS.length,\n  value,\n  onChange,\n  onComplete,\n  title = DEFAULTS.title,\n  detail,\n  label = DEFAULTS.label,\n  invalid = false,\n  reason,\n  resendIn,\n  onResend,\n  resendLabel = DEFAULTS.resendLabel,\n  busy = false,\n  children,\n}: CodeEntryProps) {\n  // Held here only when the host does not hold it. Component state, in memory,\n  // for the length of the render: nothing is written anywhere.\n  const [typed, setTyped] = React.useState('')\n  const controlled = value !== undefined\n  const code = (controlled ? value : typed).slice(0, length)\n\n  const fieldId = React.useId()\n  const reasonId = `${fieldId}-reason`\n\n  // Fired once per completed code rather than on every keystroke that leaves it\n  // complete, so a player correcting the last character does not send twice.\n  const reported = React.useRef<string | null>(null)\n\n  function changed(next: string) {\n    const cleaned = next.replace(/\\s/g, '').slice(0, length)\n    if (!controlled) setTyped(cleaned)\n    onChange?.(cleaned)\n    if (cleaned.length < length) reported.current = null\n    if (cleaned.length === length && reported.current !== cleaned) {\n      reported.current = cleaned\n      onComplete?.(cleaned)\n    }\n  }\n\n  const waiting = resendIn !== undefined && resendIn > 0\n  const boxes = Array.from({ length }, (_, index) => index)\n\n  // Where the caret is, as a box. Clamped to the last box, because a full code\n  // would otherwise put it one past the end and leave every box unmarked: no\n  // \"you are here\" and, since the focus ring hangs off it, no focus ring either,\n  // exactly when a player is tabbing back to correct a refused code.\n  const at = Math.min(code.length, length - 1)\n\n  return (\n    <section\n      className=\"gs-code-entry\"\n      data-gs-component=\"code-entry\"\n      data-gs-invalid={invalid ? 'true' : undefined}\n      data-gs-busy={busy ? 'true' : undefined}\n      // The machine-readable state of the code: how many characters are in, out\n      // of how many. A test reads these rather than counting filled boxes.\n      data-gs-value={String(code.length)}\n      data-gs-max={String(length)}\n      aria-label={typeof title === 'string' ? title : DEFAULTS.title}\n    >\n      <h2 className=\"gs-code-entry__title\" data-gs-scope=\"code-entry\" data-gs-part=\"label\">\n        {title}\n      </h2>\n      {detail ? (\n        <p className=\"gs-code-entry__detail\" data-gs-scope=\"code-entry\" data-gs-part=\"detail\">\n          {detail}\n        </p>\n      ) : null}\n\n      <div className=\"gs-code-entry__field\">\n        {/* One real input, over the boxes and invisible. It keeps the label, the\n            autofill and paste; the boxes are the picture of what it holds. */}\n        <input\n          id={fieldId}\n          className=\"gs-code-entry__input\"\n          data-gs-scope=\"code-entry\"\n          data-gs-part=\"control\"\n          data-gs-elevation=\"inset\"\n          type=\"text\"\n          inputMode=\"numeric\"\n          autoComplete=\"one-time-code\"\n          spellCheck={false}\n          maxLength={length}\n          value={code}\n          disabled={busy}\n          aria-label={label}\n          aria-invalid={invalid || undefined}\n          aria-describedby={reason ? reasonId : undefined}\n          onChange={(event) => changed(event.target.value)}\n        />\n        {/* Decorative: the input above is the field, and announcing six empty\n            boxes as well would read the same code twice. */}\n        <div className=\"gs-code-entry__boxes\" aria-hidden=\"true\">\n          {boxes.map((index) => (\n            <span\n              key={index}\n              className=\"gs-code-entry__box\"\n              data-gs-scope=\"code-entry\"\n              data-gs-part={index + 1}\n              data-gs-active={index === at ? 'true' : undefined}\n            >\n              {code[index] ?? ''}\n            </span>\n          ))}\n        </div>\n      </div>\n\n      {reason ? (\n        <p\n          className=\"gs-code-entry__reason\"\n          id={reasonId}\n          data-gs-scope=\"code-entry\"\n          data-gs-part=\"reason\"\n          role={invalid ? 'alert' : undefined}\n        >\n          {reason}\n        </p>\n      ) : null}\n\n      {onResend ? (\n        <div className=\"gs-code-entry__resend\">\n          <Button\n            variant=\"secondary\"\n            data-gs-scope=\"code-entry\"\n            data-gs-part=\"resend\"\n            // Available later, not now: it keeps its shape and stays reachable,\n            // which is how a player learns it is coming back rather than gone.\n            locked={waiting}\n            disabled={busy}\n            onClick={() => onResend()}\n          >\n            {waiting ? `Resend in ${countdown(resendIn)}` : resendLabel}\n          </Button>\n        </div>\n      ) : null}\n\n      {children}\n    </section>\n  )\n}\n",
      "type": "registry:ui",
      "target": "components/gamestage/code-entry.tsx"
    },
    {
      "path": "packages/ui/src/code-entry/code-entry.css",
      "content": "/* CodeEntry. Every value comes from a token; nothing here is a literal colour. */\n\n.gs-code-entry {\n  display: flex;\n  flex-direction: column;\n  gap: var(--gs-space-3);\n  font-family: var(--gs-type-body-font-family);\n  color: var(--gs-color-ink);\n}\n\n.gs-code-entry__title {\n  margin: 0;\n  /* The title scale. See the note in form.css: the display scale is 56px and a\n     heading over a code field is not a hero number. */\n  font-family: var(--gs-type-title-font-family);\n  font-size: var(--gs-type-title-font-size);\n  font-weight: var(--gs-type-title-font-weight);\n  line-height: var(--gs-type-title-line-height);\n}\n\n.gs-code-entry__detail {\n  margin: 0;\n  color: var(--gs-color-ink-secondary);\n  font-size: var(--gs-type-body-font-size);\n  line-height: var(--gs-type-body-line-height);\n  overflow-wrap: anywhere;\n}\n\n/* -- the field ------------------------------------------------------------- */\n/* One input, laid over the boxes and invisible. It keeps the caret, the paste\n   and the browser's one-time-code autofill; the boxes are the rendering. */\n.gs-code-entry__field {\n  position: relative;\n}\n\n.gs-code-entry__input {\n  position: absolute;\n  inset: 0;\n  inline-size: 100%;\n  block-size: 100%;\n  border: 0;\n  padding: 0;\n  background: none;\n  color: transparent;\n  caret-color: transparent;\n  font-size: var(--gs-type-body-font-size);\n  outline: none;\n  z-index: 1;\n}\n\n.gs-code-entry__boxes {\n  display: flex;\n  gap: var(--gs-space-2);\n}\n\n.gs-code-entry__box {\n  flex: 1 1 0;\n  display: flex;\n  align-items: center;\n  justify-content: center;\n\n  /* Inset, because this is typed into. BDS-040. */\n  min-inline-size: var(--gs-size-icon-object);\n  block-size: var(--gs-size-action);\n  background: var(--gs-color-surface);\n  border: var(--gs-stroke-control) solid var(--gs-color-edge);\n  border-radius: var(--gs-radius-md);\n  box-shadow: var(--gs-depth-flat);\n\n  font-family: var(--gs-type-score-font-family);\n  font-size: var(--gs-type-stat-font-size);\n  font-weight: var(--gs-type-score-font-weight);\n  font-variant-numeric: var(--gs-type-score-font-variant-numeric);\n\n  transition: border-color var(--gs-motion-emphasis) ease-out;\n}\n\n/* The box the next character lands in. The input above it holds the caret, so\n   without this there is nothing on screen saying where typing goes. */\n.gs-code-entry__box[data-gs-active='true'] {\n  border-color: var(--gs-color-selected);\n}\n\n.gs-code-entry__input:focus-visible ~ .gs-code-entry__boxes .gs-code-entry__box[data-gs-active='true'] {\n  box-shadow: 0 0 0 var(--gs-stroke-focus) var(--gs-color-selected);\n}\n\n/* -- refused --------------------------------------------------------------- */\n/* Dashed, negative and said in words below. Three changes, so a player who\n   cannot tell the two colours apart still sees that the code was wrong. */\n.gs-code-entry[data-gs-invalid='true'] .gs-code-entry__box {\n  border-style: dashed;\n  border-color: var(--gs-color-negative);\n}\n\n.gs-code-entry__reason {\n  margin: 0;\n  color: var(--gs-color-ink-secondary);\n  font-size: var(--gs-type-label-font-size);\n  line-height: var(--gs-type-body-line-height);\n}\n\n.gs-code-entry[data-gs-invalid='true'] .gs-code-entry__reason {\n  color: var(--gs-color-negative);\n  font-weight: var(--gs-type-label-font-weight);\n}\n\n/* -- resend ---------------------------------------------------------------- */\n.gs-code-entry__resend {\n  display: flex;\n  justify-content: flex-start;\n}\n",
      "type": "registry:style",
      "target": "components/gamestage/code-entry.css"
    }
  ],
  "docs": "Pass `onComplete` and send the code from there; `onChange` is there when you want every keystroke. `length` defaults to 6. Hold `value` yourself when you want to clear the boxes after a refusal, or leave it out and the component holds it in memory. Set `invalid` and `reason` when your check comes back wrong, and `busy` while it is in flight. For the resend, count down in your own state and pass `resendIn` in seconds with `onResend`: above zero the control is locked and says how long, at zero it is available. Read the state with `[data-gs-value]` against `[data-gs-max]`, and find a box with `[data-gs-scope=\"code-entry\"][data-gs-part=\"3\"]`.",
  "categories": [
    "Application surfaces"
  ],
  "type": "registry:ui"
}