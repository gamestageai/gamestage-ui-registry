{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "slider",
  "title": "Slider",
  "description": "The prediction slider. It snaps to a configurable interval, five by default, so a prediction of 47 and a prediction of 48 are the same prediction and no host has to bucket them afterwards. Moving it is not answering with it: a moved thumb is `data-gs-selected`, picked and reversible, and a separate commit control makes it `data-gs-committed`. The track is inset and the thumb is raised, which is the one rule that tells a player where to type from where to press. Keyboard-operable by the increment, with Home and End, and `data-gs-value` carrying the machine-readable number alongside whatever is rendered. Performs nothing: it does not score the prediction, settle it or send it anywhere.",
  "dependencies": [
    "react"
  ],
  "registryDependencies": [
    "@gamestage/glyphs",
    "@gamestage/tokens"
  ],
  "files": [
    {
      "path": "packages/ui/src/slider/slider.tsx",
      "content": "'use client'\n\nimport * as React from 'react'\n\n// The component brings its own styles. Without this a `gamestage-ui add` installs a\n// working but completely unstyled Slider, and `tsc --noEmit` passes on it, so\n// the gate cannot catch it.\nimport './slider.css'\n\nimport { LockGlyph } from './glyphs'\n\n/**\n * Slider: the prediction slider.\n *\n * A player drags to a number and then commits it. Those are two acts, not one\n * (BDS-014, REQ-026): moving the thumb is thinking out loud, pressing the\n * commit control is answering. A slider that answered on release would settle\n * every prediction at whatever number the thumb happened to be under when the\n * finger left the glass, which is the wrong answer more often than it is the\n * right one.\n *\n * So a moved but uncommitted slider is `data-gs-selected`, exactly as a picked\n * option is: picked, reversible. `data-gs-committed` is locked in. There is no\n * third \"pending\" state, because there is no third thing to be.\n *\n * It snaps. The value only ever lands on a multiple of `increment` from `min`,\n * five by default, because a prediction of 47 and a prediction of 48 are the\n * same prediction and a host that has to bucket them afterwards is doing the\n * component's job.\n *\n * It performs nothing. It does not know whether the prediction is right, score\n * it, or send it anywhere. The host takes `onCommitted` and settles.\n */\n\nexport type SliderCommits = 'separate' | 'immediate'\n\nexport interface SliderProps\n  extends Omit<\n    React.HTMLAttributes<HTMLDivElement>,\n    'children' | 'onChange' | 'defaultValue' | 'role'\n  > {\n  /** The name of the thing being predicted. A slider with no name is unusable. */\n  label: string\n  /** The floor. */\n  min?: number\n  /** The ceiling. */\n  max?: number\n  /** The interval the value snaps to. Five, per BDS-014, unless a host says otherwise. */\n  increment?: number\n  /** The starting value, for an uncontrolled slider. Snapped like any other. */\n  defaultValue?: number\n  /** The value, when the host owns it. Snapped like any other. */\n  value?: number\n  /**\n   * Whether committing is its own act. `separate` renders a commit control and\n   * moving the thumb answers nothing. `immediate` is for the rare surface where\n   * the drag *is* the answer, a volume or a brightness rather than a prediction.\n   */\n  commits?: SliderCommits\n  /** The word on the commit control. */\n  commitLabel?: string\n  /**\n   * The words on the commit control once the answer is in, followed by the\n   * value: \"Locked in · 25 laps\". Specimen 1i keeps the control on screen after\n   * the commit, in the positive colour, so the player can see what they locked.\n   */\n  committedLabel?: string\n  /**\n   * The unit the number is in, \"laps\" or \"goals\". Rendered small beside the\n   * value and after each end of the range, as the drawing does. Absent means a\n   * bare number.\n   */\n  unit?: string\n  /**\n   * The answer is already in. A host that re-renders a prediction the player\n   * made earlier passes this, and the slider renders the committed state\n   * without the player pressing anything.\n   */\n  committed?: boolean\n  /** Render the value as something other than the bare number, e.g. `45%`. */\n  format?: (value: number) => string\n  /** Not available at all. Goes flat, takes no focus. */\n  disabled?: boolean\n  /**\n   * Available later, not now. Keeps its shape, shows a padlock, stays\n   * focusable. Ignored when `disabled` is also set, for the reason `button`\n   * gives: a control cannot be flat and keep its shape at once.\n   */\n  locked?: boolean\n  /** Fired for every snapped value the player lands on, drag or keyboard. */\n  onValueChange?: (value: number) => void\n  /** Fired once, when the player answers. */\n  onCommitted?: (value: number) => void\n}\n\n/**\n * The words that go with the states. Taken from `choice`, deliberately: a\n * picked option and a moved slider are the same state, and two vocabularies for\n * one state is how a screen reader user learns the library is two libraries.\n */\nconst STATE_LABEL = {\n  selected: 'selected',\n  committed: 'locked in',\n  locked: 'locked',\n} as const\n\n// The glyph, because a state that is only a colour is not a state, is\n// `LockGlyph` from `@gamestage/glyphs`. It was a colour emoji, which ignored\n// every colour token and changed silhouette per operating system (GSUI-103).\n// The character is not repeated here: the gate greps the source for emoji, and\n// a mention would read as a use.\n\n/** Land on the grid, then on the range. Never outside either. */\nfunction snapTo(raw: number, min: number, max: number, increment: number): number {\n  const step = increment > 0 ? increment : 1\n  const clamped = Math.min(max, Math.max(min, raw))\n  const snapped = min + Math.round((clamped - min) / step) * step\n  // A fractional increment accumulates float error over a drag, and 44.99999\n  // renders as 44.99999. Five places is past anything a prediction needs.\n  const tidy = Number(snapped.toFixed(5))\n  return Math.min(max, Math.max(min, tidy))\n}\n\nexport function Slider({\n  label,\n  min = 0,\n  max = 100,\n  increment = 5,\n  defaultValue,\n  value,\n  commits = 'separate',\n  commitLabel = 'Lock it in',\n  committedLabel = 'Locked in',\n  unit,\n  committed: committedProp = false,\n  format,\n  disabled = false,\n  locked = false,\n  onValueChange,\n  onCommitted,\n  className,\n  ...rest\n}: SliderProps) {\n  const isLocked = locked && !disabled\n\n  const [internal, setInternal] = React.useState(() =>\n    snapTo(defaultValue ?? min, min, max, increment),\n  )\n  const [moved, setMoved] = React.useState(false)\n  const [committedHere, setCommitted] = React.useState(false)\n  const committed = committedProp || committedHere\n  const dragging = React.useRef(false)\n\n  const controlled = value !== undefined\n  const current = snapTo(controlled ? value : internal, min, max, increment)\n\n  // Committed is the end of the road: the answer is in, and the contract says\n  // it is no longer reversible. Disabled and locked are the host's two ways of\n  // saying \"not now\", which `button` already separates.\n  const frozen = disabled || isLocked || committed\n\n  const state = committed ? 'committed' : moved ? 'selected' : undefined\n\n  const rendered = format ? format(current) : String(current)\n  const withUnit = (text: string) => (unit ? `${text} ${unit}` : text)\n\n  // The name carries the state, because a screen reader user gets no fill, no\n  // thumb travel and no glyph. The number is not in here: `aria-valuenow` and\n  // `aria-valuetext` carry it, and putting it in the name as well makes every\n  // arrow key read the value twice.\n  const words = [label]\n  if (state) words.push(STATE_LABEL[state])\n  if (isLocked) words.push(STATE_LABEL.locked)\n\n  function change(next: number) {\n    if (frozen || next === current) return\n    if (!controlled) setInternal(next)\n    setMoved(true)\n    onValueChange?.(next)\n    // `immediate` means the move is the answer. It still passes through the\n    // same snap, so an immediate slider cannot report a value a separate one\n    // could not.\n    if (commits === 'immediate') {\n      setCommitted(true)\n      onCommitted?.(next)\n    }\n  }\n\n  // The value on screen is the prediction, whether or not the player dragged to\n  // it: specimen 1i draws the commit control from the start, and a player who\n  // agrees with the starting number should not have to nudge it to be allowed\n  // to answer. The only guard is that an answer is given once.\n  function commit() {\n    if (frozen) return\n    setCommitted(true)\n    onCommitted?.(current)\n  }\n\n  function onKeyDown(event: React.KeyboardEvent<HTMLDivElement>) {\n    if (frozen) return\n    const step = increment > 0 ? increment : 1\n    let next: number | undefined\n    switch (event.key) {\n      case 'ArrowRight':\n      case 'ArrowUp':\n        next = current + step\n        break\n      case 'ArrowLeft':\n      case 'ArrowDown':\n        next = current - step\n        break\n      case 'Home':\n        next = min\n        break\n      case 'End':\n        next = max\n        break\n      default:\n        return\n    }\n    // The page scrolls on arrow keys and jumps to the top on Home, and a player\n    // holding an arrow to reach 80 would lose the slider off the bottom of the\n    // screen.\n    event.preventDefault()\n    change(snapTo(next, min, max, increment))\n  }\n\n  function valueFromPointer(\n    event: React.PointerEvent<HTMLDivElement>,\n  ): number | undefined {\n    const rect = event.currentTarget.getBoundingClientRect()\n    // A zero-width track means the element is not laid out yet. Dividing by it\n    // yields Infinity, which snaps to max, so the first pointer event on an\n    // unlaid-out slider would answer the maximum.\n    if (rect.width === 0) return undefined\n    const fraction = (event.clientX - rect.left) / rect.width\n    return snapTo(min + fraction * (max - min), min, max, increment)\n  }\n\n  function onPointerDown(event: React.PointerEvent<HTMLDivElement>) {\n    if (frozen) return\n    dragging.current = true\n    const next = valueFromPointer(event)\n    if (next !== undefined) change(next)\n  }\n\n  function onPointerMove(event: React.PointerEvent<HTMLDivElement>) {\n    if (frozen || !dragging.current) return\n    const next = valueFromPointer(event)\n    if (next !== undefined) change(next)\n  }\n\n  function endDrag() {\n    dragging.current = false\n  }\n\n  // Unitless, 0 to 1, so the stylesheet can write `(100% - thumb) * ratio` and\n  // the thumb stays inside the bar at both ends. A percentage could not: CSS\n  // cannot divide one by another.\n  const span = max - min\n  const ratio = span > 0 ? (current - min) / span : 0\n\n  return (\n    <div\n      {...rest}\n      className={['gs-slider', className].filter(Boolean).join(' ')}\n      data-gs-component=\"slider\"\n      data-gs-value={current}\n      data-gs-min={min}\n      data-gs-max={max}\n      data-gs-increment={increment}\n      data-gs-commits={commits}\n      data-gs-selected={state === 'selected' ? 'true' : undefined}\n      data-gs-committed={state === 'committed' ? 'true' : undefined}\n      data-gs-disabled={disabled ? 'true' : undefined}\n      data-gs-locked={isLocked ? 'true' : undefined}\n    >\n      <span className=\"gs-slider__label\" data-gs-scope=\"slider\" data-gs-part=\"label\" aria-hidden=\"true\">\n        {label}\n      </span>\n\n      {/* The arrangement is specimen 1i's: the value floats in a bubble over\n          the thumb, the track carries five ticks, and the ends of the range\n          are written under it. The bubble and the thumb share one position,\n          the ratio, so they cannot drift apart. */}\n      <div\n        className=\"gs-slider__field\"\n        // The ratio is data, not design: it is the only number in this\n        // component a stylesheet cannot derive, so it arrives as a custom\n        // property and the CSS keeps every colour and size in tokens.\n        style={{ '--gs-slider-ratio': ratio } as React.CSSProperties}\n      >\n        <span className=\"gs-slider__readout\" data-gs-animates=\"motion\" aria-hidden=\"true\">\n          <span className=\"gs-slider__value gs-display-type\" data-gs-scope=\"slider\" data-gs-part=\"value\">\n            {rendered}\n          </span>\n          {unit ? <span className=\"gs-slider__unit\">{unit}</span> : null}\n        </span>\n\n        {/* The widget, and only the widget. `role=\"slider\"` takes no interactive\n            children, so the commit control is a sibling below rather than a\n            child of this element. */}\n        <div\n          className=\"gs-slider__track\"\n          data-gs-scope=\"slider\" data-gs-part=\"track\"\n          // The track is an input, so it is inset; the thumb is a control, so it\n          // is raised. BDS-040's whole rule, in one component, made assertable.\n          data-gs-elevation=\"inset\"\n          role=\"slider\"\n          tabIndex={disabled ? -1 : 0}\n          aria-label={words.join(', ')}\n          aria-valuenow={current}\n          aria-valuemin={min}\n          aria-valuemax={max}\n          aria-valuetext={format || unit ? withUnit(rendered) : undefined}\n          // Horizontal only. A vertical prediction slider would need an attribute\n          // to be found by and the contract allocates none, so it is not offered\n          // rather than offered unassertably.\n          aria-orientation=\"horizontal\"\n          aria-disabled={frozen || undefined}\n          onKeyDown={onKeyDown}\n          onPointerDown={onPointerDown}\n          onPointerMove={onPointerMove}\n          onPointerUp={endDrag}\n          onPointerCancel={endDrag}\n          onLostPointerCapture={endDrag}\n        >\n          <span className=\"gs-slider__fill\" data-gs-scope=\"slider\" data-gs-part=\"fill\" data-gs-animates=\"motion\" />\n          {/* Five ticks, as drawn: the snap grid made visible at a glance.\n              Decorative, so no part and no name. */}\n          <span className=\"gs-slider__ticks\" aria-hidden=\"true\">\n            <i /><i /><i /><i /><i />\n          </span>\n          <span\n            className=\"gs-slider__thumb\"\n            data-gs-scope=\"slider\" data-gs-part=\"thumb\"\n            data-gs-elevation=\"raised\"\n            // The thumb travels and does nothing else, so it is motion and stops\n            // dead under reduced motion. The fill's colour and the committed\n            // treatment are emphasis and survive, which is how a player who\n            // cannot see it move still sees where it got to.\n            data-gs-animates=\"motion\"\n          >\n            {/* Three grip dots, the drawing's handle texture. */}\n            <i /><i /><i />\n          </span>\n        </div>\n\n        <span className=\"gs-slider__range\" aria-hidden=\"true\">\n          <span>{withUnit(format ? format(min) : String(min))}</span>\n          <span>{withUnit(format ? format(max) : String(max))}</span>\n        </span>\n      </div>\n\n      {isLocked ? (\n        <span\n          className=\"gs-slider__glyph\"\n          data-gs-scope=\"slider\" data-gs-part=\"glyph\"\n          data-gs-animates=\"emphasis\"\n          aria-hidden=\"true\"\n        >\n          <LockGlyph />\n        </span>\n      ) : null}\n\n      {/* Always on screen for a separate commit, as specimen 1i draws it: it\n          asks for the answer before, and records it after. Disabled rather\n          than removed once the answer is in, so the record stays legible and\n          the focus does not fall off the page. */}\n      {commits === 'separate' ? (\n        <button\n          type=\"button\"\n          className=\"gs-slider__commit gs-action-type gs-pressable\"\n          data-gs-scope=\"slider\" data-gs-part=\"commit\"\n          data-gs-animates=\"motion\"\n          data-gs-disabled={frozen ? 'true' : undefined}\n          aria-disabled={frozen || undefined}\n          disabled={frozen}\n          onClick={commit}\n        >\n          {committed ? `${committedLabel} · ${withUnit(rendered)}` : commitLabel}\n        </button>\n      ) : null}\n    </div>\n  )\n}\n",
      "type": "registry:ui",
      "target": "components/gamestage/slider.tsx"
    },
    {
      "path": "packages/ui/src/slider/slider.css",
      "content": "/* Slider. Every value comes from a token; nothing here is a literal colour.\n\n   The arrangement is specimen 1i's and design/twins/1i.json measures every\n   part of it against the drawing: a value bubble floating over the thumb, a\n   bordered pill track with five ticks, a 52px thumb with three grips, the two\n   ends of the range written under the track, and a full-width commit control\n   that stays on screen and turns positive once the answer is in. */\n\n.gs-slider {\n  display: grid;\n  grid-template-areas: 'label glyph' 'field field' 'commit commit';\n  grid-template-columns: 1fr auto;\n  align-items: center;\n  gap: var(--gs-space-2);\n  width: 100%;\n}\n\n.gs-slider__label {\n  grid-area: label;\n  font-family: var(--gs-type-label-font-family);\n  font-size: var(--gs-type-label-font-size);\n  font-weight: var(--gs-type-label-font-weight);\n  letter-spacing: var(--gs-type-label-letter-spacing);\n  text-transform: uppercase;\n  color: var(--gs-color-ink-secondary);\n}\n\n/* -- the field: bubble, track, range ---------------------------------------- */\n/* Room above the track for the bubble, which is absolutely placed so that it\n   can sit over the thumb without the track moving under it. The drawing pads\n   56px, which is the action height. */\n.gs-slider__field {\n  grid-area: field;\n  position: relative;\n  padding-top: var(--gs-size-action);\n  user-select: none;\n}\n\n/* The readout, as drawn: a raised bubble ringed at the UI stroke, centred on\n   the thumb, the number and its unit on one baseline. It is the one piece of\n   this component that floats, so it carries the floating depth. */\n.gs-slider__readout {\n  position: absolute;\n  top: 0;\n  left: calc(100% * var(--gs-slider-ratio, 0));\n  transform: translateX(-50%);\n  display: flex;\n  align-items: baseline;\n  white-space: nowrap;\n  padding: var(--gs-space-2) var(--gs-space-inset);\n  color: var(--gs-color-ink);\n  background: var(--gs-color-surface-raised);\n  border: var(--gs-stroke-ui) solid var(--gs-color-edge-high);\n  border-radius: var(--gs-radius-md);\n  box-shadow: var(--gs-depth-floating);\n  transition: left var(--gs-motion-press) ease-out;\n}\n\n/* Tabular numerals, or the readout shifts under the thumb on every snap and the\n   number reads as jittering rather than changing. */\n.gs-slider__value {\n  font-family: var(--gs-type-stat-font-family);\n  font-size: var(--gs-type-stat-font-size);\n  font-weight: var(--gs-type-stat-font-weight);\n  font-variant-numeric: var(--gs-type-score-font-variant-numeric);\n  line-height: 1;\n  text-shadow: none;\n  transition: color var(--gs-motion-emphasis) ease-out;\n}\n\n.gs-slider__unit {\n  margin-left: var(--gs-space-1);\n  font-family: var(--gs-type-caption-font-family);\n  font-size: var(--gs-type-caption-font-size);\n  font-weight: var(--gs-type-caption-font-weight);\n  color: var(--gs-color-ink-secondary);\n}\n\n/* -- the track: inset, because it is an input (BDS-040) -------------------- */\n/* A bordered pill, drawn at the surface colour inside the UI stroke. The box\n   is 24px outside to outside, which is what the drawing's 20px content plus its\n   2px ring comes to. */\n.gs-slider__track {\n  position: relative;\n  box-sizing: border-box;\n  display: block;\n  width: 100%;\n  height: var(--gs-space-6);\n  background: var(--gs-color-surface);\n  border: var(--gs-stroke-ui) solid var(--gs-color-edge);\n  border-radius: var(--gs-radius-full);\n  cursor: pointer;\n  /* Or the browser scrolls the page instead of letting the drag through. */\n  touch-action: none;\n}\n\n/* The drawn bar is 24px tall and a thumb needs 48. The hit area is extended\n   above and below without changing what is drawn: a pointer landing on this\n   pseudo-element lands on the track. */\n.gs-slider__track::after {\n  content: '';\n  position: absolute;\n  inset-inline: 0;\n  inset-block: calc((var(--gs-size-touch-min) - var(--gs-space-6)) / -2);\n}\n\n.gs-slider__track:focus-visible {\n  outline: var(--gs-stroke-focus) solid var(--gs-color-ink);\n  outline-offset: var(--gs-space-1);\n}\n\n/* The fill runs from the left edge to the thumb's centre. */\n.gs-slider__fill {\n  position: absolute;\n  inset-inline-start: 0;\n  top: 0;\n  bottom: 0;\n  width: calc(100% * var(--gs-slider-ratio, 0));\n  background: var(--gs-color-interactive);\n  border-radius: var(--gs-radius-full);\n  transition: width var(--gs-motion-press) ease-out;\n}\n\n/* Five ticks spaced across the bar, inset by a space-6 from each end so the\n   outer two sit under the thumb's edge rather than its centre. */\n.gs-slider__ticks {\n  position: absolute;\n  inset: 0;\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n  padding: 0 var(--gs-space-6);\n  pointer-events: none;\n}\n\n.gs-slider__ticks i {\n  display: block;\n  width: var(--gs-space-1);\n  height: var(--gs-space-1);\n  border-radius: var(--gs-radius-full);\n  background: var(--gs-color-edge-high);\n}\n\n/* -- the thumb: raised, because it is a control (BDS-040) ------------------ */\n/* A chip-sized disc in the interactive colour, centred on the ratio and hanging\n   past the bar at either end, as drawn. It presses like every other control:\n   the pressable depth is the highlight and the offset base. */\n.gs-slider__thumb {\n  position: absolute;\n  top: 50%;\n  inset-inline-start: calc(100% * var(--gs-slider-ratio, 0));\n  width: var(--gs-size-chip);\n  height: var(--gs-size-chip);\n  transform: translate(-50%, -50%);\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  gap: var(--gs-space-1);\n  color: var(--gs-color-on-accent);\n  background: var(--gs-color-interactive);\n  border-radius: var(--gs-radius-full);\n  box-shadow: var(--gs-depth-pressable);\n  cursor: grab;\n  transition: inset-inline-start var(--gs-motion-press) ease-out,\n              box-shadow var(--gs-motion-press) ease-out;\n}\n\n/* Three grip dots in the thumb's own ink. */\n.gs-slider__thumb i {\n  display: block;\n  width: var(--gs-space-1);\n  height: var(--gs-space-1);\n  border-radius: var(--gs-radius-full);\n  background: currentColor;\n  opacity: 0.7;\n}\n\n/* Pressable things move, they never fade (BDS-013). The thumb has the depth it\n   has, so it travels that far and lands flat. */\n.gs-slider__track:active:not([aria-disabled='true']) .gs-slider__thumb {\n  box-shadow: var(--gs-depth-pressable-pressed);\n  cursor: grabbing;\n}\n\n/* The two ends of the range, under the track. */\n.gs-slider__range {\n  display: flex;\n  justify-content: space-between;\n  margin-top: var(--gs-space-inset);\n  font-family: var(--gs-type-label-font-family);\n  font-size: var(--gs-type-label-font-size);\n  font-weight: var(--gs-type-label-font-weight);\n  color: var(--gs-color-muted);\n}\n\n/* -- selected and committed ----------------------------------------------- */\n/* Moved but not answered carries `data-gs-selected` and draws nothing extra:\n   specimen 1i keeps the interactive colour through the drag, and the thumb's\n   position is the record of the move. The attribute is there for a host that\n   wants to draw it. */\n\n/* Answered. Bubble, fill, thumb and commit all take the positive colour at\n   once, and the accessible name carries the word, so no player is told by\n   colour alone. The offset base under the thumb and the commit follows the\n   positive base, because the pressable depth is composed from\n   `--gs-color-interactive-base` and this is the one place it is re-pointed. */\n.gs-slider[data-gs-committed='true'] .gs-slider__readout {\n  color: var(--gs-color-on-bright);\n  background: var(--gs-color-positive);\n  border-color: var(--gs-color-positive-solid);\n}\n\n.gs-slider[data-gs-committed='true'] .gs-slider__unit {\n  color: var(--gs-color-on-bright);\n}\n\n.gs-slider[data-gs-committed='true'] .gs-slider__fill {\n  background: var(--gs-color-positive);\n}\n\n.gs-slider[data-gs-committed='true'] .gs-slider__thumb {\n  --gs-color-interactive-base: var(--gs-color-positive-base);\n  color: var(--gs-color-on-bright);\n  background: var(--gs-color-positive);\n}\n\n.gs-slider[data-gs-committed='true'] .gs-slider__track { cursor: default; }\n\n/* -- locked and disabled -------------------------------------------------- */\n/* Locked keeps its shape and shows a padlock: available later, not now. */\n.gs-slider[data-gs-locked='true'] .gs-slider__track { cursor: not-allowed; }\n\n.gs-slider__glyph {\n  grid-area: glyph;\n  font-size: var(--gs-size-icon-control);\n  line-height: 1;\n  /* Emphasis, not motion: this duration survives reduced motion on purpose. */\n  transition: opacity var(--gs-motion-emphasis) ease-out;\n}\n\n/* Only disabled goes flat: not available at all. */\n.gs-slider[data-gs-disabled='true'] .gs-slider__label,\n.gs-slider[data-gs-disabled='true'] .gs-slider__range {\n  color: var(--gs-color-disabled-ink);\n}\n\n.gs-slider[data-gs-disabled='true'] .gs-slider__readout {\n  color: var(--gs-color-disabled-ink);\n  background: var(--gs-color-disabled);\n  border-color: var(--gs-color-disabled);\n  box-shadow: var(--gs-depth-flat);\n}\n\n.gs-slider[data-gs-disabled='true'] .gs-slider__fill {\n  background: var(--gs-color-disabled);\n}\n\n.gs-slider[data-gs-disabled='true'] .gs-slider__thumb {\n  color: var(--gs-color-disabled-ink);\n  background: var(--gs-color-disabled);\n  box-shadow: var(--gs-depth-flat);\n}\n\n.gs-slider[data-gs-disabled='true'] .gs-slider__track { cursor: not-allowed; }\n\n/* -- the commit control --------------------------------------------------- */\n/* Its own control rather than an imported Button, so the item depends on the\n   tokens and nothing else. Full width at the action height, as drawn, and it\n   presses the way BDS-013 says every control does: the offset base collapses\n   and the face travels the same distance. */\n.gs-slider__commit {\n  grid-area: commit;\n  box-sizing: border-box;\n  width: 100%;\n  height: var(--gs-size-action);\n  margin-top: var(--gs-space-2);\n  padding: 0 var(--gs-space-6);\n\n  color: var(--gs-color-on-accent);\n  background: var(--gs-color-interactive);\n  border: 0;\n  border-radius: var(--gs-radius-control);\n  box-shadow: var(--gs-depth-pressable);\n  cursor: pointer;\n\n  transition: transform var(--gs-motion-press) ease-out,\n              box-shadow var(--gs-motion-press) ease-out,\n              background-color var(--gs-motion-emphasis) ease-out;\n}\n\n.gs-slider__commit:hover:not([aria-disabled='true']) { background: var(--gs-color-interactive-hover); }\n\n.gs-slider__commit:active:not([aria-disabled='true']) {\n  transform: var(--gs-press-transform);\n  box-shadow: var(--gs-depth-pressable-pressed);\n}\n\n.gs-slider__commit:focus-visible {\n  outline: var(--gs-stroke-focus) solid var(--gs-color-ink);\n  outline-offset: 3px;\n}\n\n/* Locked in: the control records the answer in the positive colour and stops\n   being a button in everything but shape. */\n.gs-slider[data-gs-committed='true'] .gs-slider__commit {\n  --gs-color-interactive-base: var(--gs-color-positive-base);\n  color: var(--gs-color-on-bright);\n  background: var(--gs-color-positive);\n  cursor: default;\n}\n\n/* Not now: the control stays where it is and goes flat. */\n.gs-slider[data-gs-disabled='true'] .gs-slider__commit,\n.gs-slider[data-gs-locked='true'] .gs-slider__commit {\n  color: var(--gs-color-disabled-ink);\n  background: var(--gs-color-disabled);\n  box-shadow: var(--gs-depth-flat);\n  cursor: not-allowed;\n}\n",
      "type": "registry:style",
      "target": "components/gamestage/slider.css"
    }
  ],
  "docs": "Pass `label` to name the thing being predicted; a slider with no name is unusable with a screen reader. Pass `increment` to change the snap interval, which is five per BDS-014. `commits` defaults to `\"separate\"`, which renders the commit control and is what a prediction wants; `\"immediate\"` is for the rare surface where the drag itself is the answer, a volume rather than a prediction. Take the answer from `onCommitted`, never from `onValueChange`: the second one fires for every number the player passes through on the way. Pass `value` to own the number yourself, or `defaultValue` to let the slider hold it; either way it is snapped before it is rendered.",
  "categories": [
    "Controls"
  ],
  "type": "registry:ui"
}